import { answer,compact,rows,type PlanSource } from './shared.ts';
export const buildMarketingContext=({questionnaire:q,approvedFinancial}:PlanSource)=>compact({activities:rows(q,'marketingActivities').map((x:any)=>({channel:x['Marketing channel'],description:x.Description,budget:x['Monthly budget'],frequency:x.Frequency,targetCustomer:x['Target customer'],timing:x['Start date']})),customerDiscovery:answer(q,'How will customers find the business?'),channels:answer(q,'What marketing channels will be used?'),promotions:answer(q,'What promotions will be offered?'),salesProcess:answer(q,'What is the sales process?'),salesCycle:answer(q,'How long does it take to close a sale?'),retention:answer(q,'What is the expected customer-retention strategy?'),approvedAnnualMarketingSpend:approvedFinancial?.annualMarketingSpend});
