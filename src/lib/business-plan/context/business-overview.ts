import { answer,compact,limit,type PlanSource } from './shared.ts';
export const buildBusinessOverviewContext=({profile,questionnaire:q}:PlanSource)=>compact({businessName:profile.businessName||answer(q,'What is the business name?'),description:limit(profile.description||answer(q,'What does the business do?')),legalStructure:answer(q,'What is the legal structure?'),stage:profile.stage,location:answer(q,'Where will it operate?')||[profile.city,profile.region,profile.country].filter(Boolean).join(', '),operatingModel:answer(q,'Is it online, physical or both?'),startDate:answer(q,'When did or will it begin?'),shortTermGoals:limit(profile.shortGoals||answer(q,'What are the short-term goals?')),longTermGoals:limit(profile.longGoals||answer(q,'What are the long-term goals?')),needAddressed:limit(profile.problem||answer(q,'What problem or need does it address?')),differentiation:limit(profile.difference||answer(q,'What makes the business different?')),planPurpose:profile.purpose});
